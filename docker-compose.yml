services:
  spectra:
    build: .
    container_name: spectra
    ports:
      - "${SPECTRA_PORT:-8085}:8080"
    environment:
      AI_PROVIDER: ${AI_PROVIDER:-local}
    volumes:
      - /root/data:/app/data
      - /root/inbox:/app/inbox
      - /root/processed:/app/processed
    restart: unless-stopped
